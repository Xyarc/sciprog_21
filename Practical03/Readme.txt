This Practical contians a program which calculates the area under a tan function between a & b using the trapezodial method


To compile and run the programs do the following:

1) Compile Trapezodial.c with 'gcc -o <Executable name> <Filename> -lm' in the file location

2) To run program use './<Executable name>' in the file location.
