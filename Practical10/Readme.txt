This practical contains a program to solve the tower of Hanoi problem for a user specified number of disks.


To compile and run the programs do the following:

1) Compile TowerofHanoi.c with 'gcc -o <Executable name> <Filename> -lm' in the file location

2) To run program use './<Executable name>' in the file location.
