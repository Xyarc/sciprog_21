This practical contains a variation on practical 3, computing the area under a tan function between a & b now using arrays rather than adding to a single variable.


To compile and run the programs do the following:

1) Compile TrapizodialArray.c with 'gcc -o <Executable name> <Filename> -lm' in the file location

2) To run program use './<Executable name>' in the file location.
